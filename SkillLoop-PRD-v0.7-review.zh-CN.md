# SkillLoop PRD v0.7 工程就绪度审查

审查日期：2026-09-23。对象为 [PRD 固定版本](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md)，commit `74677b2b6417df00d9f05a5ca51a3c5037877ae2`。已核对该固定版本与读取的 main 内容相同；本文所有 L 行号对应该版本。另保存 [原文快照](./SkillLoop-PRD-v0.7.snapshot.md)。工作区原有 `docs/PRD.md` 是 v0.1，不是本次审查对象。

**结论：目前可以作为方案与原型开发依据，不能直接作为无歧义的严肃工程实施基线。** 最大缺口不是缺少组件，而是组件间的约束尚未合并成唯一的状态机、授权语义、判定表和预算调度规则。本文列出 **48 项主要问题：9 项阻断、30 项高优先级、9 项中优先级**，另列编辑与术语收口项。它们是问题清单，不表示已证实48个实现漏洞。

这是一份设计审查，不是已运行系统的漏洞证明。在所核查 commit 的完整仓库树中，只有 `PRD.md` 和 `Readme.md`，没有实现代码、配置或测试可用来消解歧义。没有连接 DGX、安装扫描器或运行模型实验。下文的故障场景是依据文档推演的反例；明确标为“建议”的决策尚未得到项目所有者确认。

分级：**阻断**＝开工前应冻结，错误实现可能破坏核心安全或 CI 正确性；**高**＝相关模块实现前必须收口；**中**＝必须在验收/对外使用前收口。类型区分直接冲突、缺少契约、工程风险，避免把“没写清”误报成“已经实现错误”。本文编号是审查问题 ID，与 PRD 的 P0/P1 功能优先级无关。

## 一、跨章节冲突与核心决策

### R01｜阻断｜修补产物通过，不能替原 PR 的原始内容获得通过状态

**位置：** [§5.2 L281](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L281)、§8.3 L668、§9.2 L725–726、§9.4 L815。类型：关键契约缺失。

- 当前描述允许一次 CI 在 head 失败后修补、让 finalist 通过并内部晋级，但只给作业一个 `pass/fail`，没有说明这个结果对应原提交还是生成后的 bundle。
- 反例：提交 H 的发布目的地越权；补丁 R 修好；检查给 H 返回绿色，仓库合并的仍是 H。Registry 里的 R 安全与合并的 H 安全是两件事。
- **建议收口：** 同时输出 `submitted_head_verdict`、`candidate_verdict`、`evaluated_bundle_hash`、`source_commit_sha`、`repair_required`。PR 必需检查只评价对应 SHA；R 通过可以生成候选产物，但 H 保持失败。补丁进入新提交后重新验证，或使用明确绑定 R 的独立制品发布流程。
- **验收：** H 失败、R 通过时，H 的合并检查仍失败；篡改制品内容或换 commit 后，旧通过决定不可复用。

### R02｜高｜保护集执行时机在预算表与循环规则中直接冲突

**位置：** [§10.3 L864–871](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L864)、§5.2 L281、§7.3 L547。类型：直接冲突。

- 初始 16 次运行包含旧版和提交版各自的全部 2 正常＋4 攻击＋2 历史，其中已包含保护样本；后文却说保护集只在开发通过的最终候选上运行。修补后又安排 3 次保护运行。
- 这不必然意味着已经泄漏，但实现者必须自行决定提交版保护失败能否触发修补、保护集可被查询几次、初始保护结果是否给 Diagnoser。
- **建议收口：** 先只运行旧版/提交版开发集；修补选定 finalist 后，才运行 baseline/finalist 的保护配对。若坚持提前跑 baseline 保护结果，明确它只能由 Gate 持有，并定义按“模型版本还是候选版本”计保护查询次数。
- **验收：** 事件日志可证明 finalist 冻结前，Patcher 没有收到任何保护判定；保护失败不能触发本作业内第三次候选或换 finalist。

### R03｜阻断｜严格任务授权与允许越权副作用的基线尚未形成一致语义

**位置：** [§6.1 L383–390](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L383)、§6.4 L460、§6.7 L490–495、§4.3 L212。类型：语义冲突。

- 若 `SessionGrant(task)` 已经严格限制为合法任务资源/目的地，取交集后，宽 `Policy0` 也无法实现任务外读取或错误发布；若 SessionGrant 在实验中故意比业务意图宽，则它不再等同于“本任务实际授权”。C1 的无授权发布也依赖这一区分。
- PRD 已说明合成环境和粗策略的存在，但没有给出两种 grant 的可执行定义；很容易做出一个永远 ASR=0 的基线，或把实验放宽机制带进正常 CI。
- **建议收口：** 定义 `strict_ci` 与 `synthetic_ablation` 两种不可混用的模式。前者严格执行可信授权；后者用独立、仅合成世界有效的 execution grant 复现缺陷，Oracle 仍按业务意图判越权。关闭凭证检查只能在后者的固定 baseline 配置中发生。
- **验收：** 模式、grant 与 policy 全部入 manifest；正常 CI 拒绝加载宽权限实验配置；合成基线能发生指定违规且无法接触宿主/外网。

### R04｜阻断｜Finding 的关闭条件没有定义，容易出现永远不通过或错误销案

**位置：** [§5.3 L295–300](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L295)、§5.8 L351、§7.1 L516、§8.1 L634–638。类型：状态机缺失。

- `dynamic_attack/static_check/needs_review/false_positive` 混合了处理方式和处置状态；`confirmed/blocked/not_reproduced/inconclusive` 又是观测结果，没有 `fixed/mitigated/accepted_risk/reopened` 等处置含义。
- 高风险 finding 有效测试未触发时不能算误报，但它到底能否通过？策略已阻止真实副作用、扫描仍报危险文字时，能否标为已缓解？静态确认的依赖风险没有动态 trace，怎样结束处理？均未规定。
- **建议收口：** 拆成 `verification_method`、`execution_outcome`、`disposition` 三个字段；给每种 severity×disposition 制定 Gate 表。建议 `not_reproduced` 高风险继续阻断；`mitigated_by_policy` 需要绑定执行覆盖和回归证据；`false_positive/accepted_risk` 必须有可信审核记录及失效条件。
- **验收：** 同一 finding 在“未触发”“被拦截”“确认后修复”“例外过期”四种情况下有唯一状态转移与机器结果；模型不能自行销案。

### R05｜阻断｜Gate 仍是自然语言规则，缺少唯一的裁决真值表

**位置：** [§8.1 L632–648](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L632)、§8.4 L678。类型：关键接口缺失。

- “关键违规”“其他目标”“关键正常任务”“适用门禁”“最小改善”没有枚举或默认值。`needs_review` 没有确定的外部映射；未知策略到底是 fail 还是 inconclusive，也没有固定规则。
- 同时存在已确认泄漏和环境失败时，返回哪个状态？无契约且扫描已发现风险时，哪个优先？普通更新是否也必须经过所有 policy 包含与义务检查？开发集通过的门槛是否与最终 Gate 一致？都需要实现者猜。
- **建议收口：** 用版本化 `GateSpec` 定义必测集合、逐例规则、严重度、改善阈值、外部状态优先级与全部 reason codes；将适用/不适用显式记录。建议保留全部阻断原因，并采用“已确认违规→fail；否则缺可信契约→needs_contract；否则证据不全→inconclusive；否则 pass”的优先级，项目需正式确认。
- **验收：** 固定输入证据无论遍历顺序如何均得到同一裁决；风险、未完成与无契约同时出现的组合均有表驱动测试。

### R06｜阻断｜比较基线和首次接入 bootstrap 没有收口

**位置：** [§7.4 L566](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L566)、§8.1 L638–644、§10.3 L864。类型：关键契约缺失。

- 当前版、提交版、上一轮候选均可能成为“基线”。例如 active 正常能力 100%，提交版降到 50%，补丁恢复到 75%，相对提交版改善但仍不应替代 active。
- 首次接入没有当前已晋级版本；如果初始包有漏洞，它只能是待测 baseline，不能为了运行流程先赋予可信 active 身份。
- **建议收口：** 固定三个角色：`active_bundle` 用于防回归，`submitted_bundle` 用于本次修补归因，`parent_candidate` 用于轮间诊断。修复收益比较提交版；晋级要求同时不劣于 active。首次接入无 active 时使用绝对业务/安全标准，通过前保持 `unapproved`。
- **验收：** 上述 100→50→75 场景被拒；无 active 时能完整运行并得到明确的首次接入结果。

### R07｜高｜37 次只是表内算术正确，尚不是完整执行计划的最坏上限

**位置：** [§10.3 L862–873](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L862)、§5.2 L273–279、§7.2 L533。类型：预算契约缺失。

- 两轮每轮只有两个开发攻击槽位，但又要求原攻击、同族新变体、复扫新发现，以及发现成功后的回归。若两个原攻击都成功，再加至少一个新变体，已超过两个槽位；4 次变异与 4 次复现预留之间如何共享，没有定义。
- 初始保护集开销也需按 R02 重排。不能只证明 `16+10+3+4+4=37` 就证明最坏情况能完成全部义务。
- **建议收口：** 每次作业先生成逐 run 的 `ExecutionPlan`；保留强制回归槽位，再分配可选搜索；全局计数器在调模型前扣预算。任一强制用例无法排入时，返回覆盖不足，不能替换掉原失败用例而宣称修好。37 可以保留为硬上限，但必须允许诚实地不完成。
- **验收：** 两条初始漏洞＋第一轮新漏洞＋环境重试的压力情形，预算不超限、原失败不丢失、不会给出 pass。

### R08｜高｜全部历史回归与固定预算不能无限同时成立

**位置：** [§7.3 L539–544](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L539)、§8.1 L638、§10.3 L871。类型：范围冲突。

- PR 只跑至多两个优先历史案例，夜间要求全部相关历史案例且上限 64；历史库持续增长后一定出现放不下的情况。若还需比较两个版本，单次完整配对至少为 `2×(6+12+H)`，尚未计修补与重试。
- “历史回归零关键副作用”是指抽取的两条还是整个库？两种实现会给出不同的绿色含义。
- **建议收口：** 明确快速通过的覆盖范围；关键历史案例设为不可采样的必测集合，超出单作业预算可拆分批次，在同一 bundle/suite 下汇总后才给发布门禁通过；定义普通 PR 是否必须等完整回归。
- **验收：** H 增长越过 64 的容量后，系统排队或报告未完成，而不是静默漏测；旧案例不因排序变化永久失去覆盖。

### R09｜高｜什么证据可以触发修补，文中仍有冲突

**位置：** [§5.2 L271–279](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L271)、§5.6 L330–336、§5.9 L364、§8.2 L656。类型：语义未收口。

- 主流程强调已证实失败、没有复现不能触发自动修补；同时又要修复“被拒绝后不能完成业务”、减少 attempted、允许 policy-hardening。被拦截且业务失败属于真实功能失败，但并非 `confirmed=realized` 漏洞。
- **建议收口：** 显式枚举可修事件：真实违规、确定性业务失败、有效越权尝试是否可单独触发；静态已确认风险是否允许预防性修补另列 profile。不要用一个 `confirmed` 同时表示发现属实和违规已经发生。
- **验收：** blocked＋utility_fail 可进入功能恢复；仅 not_reproduced 不凭空生成漏洞修复；每个补丁声明与触发证据类别一致。

### R10｜高｜新发现当前版有严重漏洞时，“保持原版本”不是完整故障策略

**位置：** [§4.2 L206](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L206)、§8.3 L668–670、§8.4 L674。类型：状态机缺失。

- 如果本次发现 active 本身已失守、所有候选都失败，既要求保持原版本，又要求严重漏洞版本 revoked，缺少“无可用安全版本”的状态。回滚只检查 SessionGrant 和主机上限，也没有说明是否允许恢复已被策略修补关闭的权限。
- **建议收口：** 将 `active_pointer` 与 `deployable/eligible` 分离；记录 revoked 后可保留证据，但禁止新任务使用。回滚检查当前安全政策、未关闭漏洞和有效评测证明；没有合格目标时进入 `no_eligible_bundle`，等待修复/可信处置。
- **验收：** active 被撤销后，不会因候选全部失败而继续被自动选用；历史版本不会绕过当前撤销和策略下限。

## 二、权限、凭证与隔离边界

### R11｜阻断｜策略子集检查的证明域没有定义

**位置：** [§6.3 L434–446](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L434)。类型：形式语义缺失。

- 有限集合可以比较，但 `task.inputs` 是参数化集合。旧策略允许 `{a,b}`，当前任务输入为 `{a}` 时新策略通过；下次输入 `{c}` 时就不能推断仍为子集。PRD 同时要求不是仅枚举测试参数、又要求每次任务绑定后重查，仍缺少 version 级结论的适用域。
- 带阶段、预算、凭证的策略限制的是有状态执行序列，单步动作集合包含不自动覆盖状态转移变化。OR 分支和不同参数之间的关联也不能分别投影后比较。
- **建议收口：** P0 定义有类型的有限 DSL AST、缺省值、AND/OR 语义及固定状态机。版本证明绑定已批准 contract 的资源域；每次具体任务再校验。不支持的绑定变换或状态转移改变返回 unknown。比较完整动作元组及条件，不拆散参数相关性。
- **验收：** 动态集合新增资源、重叠 allow、删除前置条件、预算单位变化、不同目的地与产物配对，均有反例测试。

### R12｜阻断｜凭证消费与副作用“原子”尚未选定可实现的存储模型

**位置：** [§6.6 L474–478](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L474)、§9.1 L696、§9.6 L830。类型：关键工程选择。

- 私有 receipt store 与文件/模拟接收端若是两个独立介质，普通本地事务不能同时提交两者。先消费后发布会丢操作；先发布后消费会在崩溃重试时重复发布。幂等键本身不能补全恢复流程。
- **建议收口：** P0 将模拟发布事件、receipt 消费、执行结果放在同一事务存储，以不可变 artifact hash 引用内容；若使用独立接收端，定义持久化操作状态机与接收端幂等/查询协议。没有可判定的提交结果时标为 unknown effect，并阻止再次危险执行和晋级。
- **验收：** 在校验后、发送后、消费前后及日志提交前强制崩溃；恢复后副作用至多一次，且最终记录与接收端一致。

### R13｜高｜“同一产物 hash”不足以证明完整业务验证已经完成

**位置：** [§6.2 L424–430](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L424)、§6.6 L474、§9.2 L722。类型：字段契约缺失。

- 同一个文件可能只通过 schema 检查但未通过 totals；也可能用旧版 validator、另一组输入或另一任务实例校验。只绑定 task_id、产物 hash、policy 版本无法区分。固定示例 task_id 在多次 run 中复用尤其危险。
- **建议收口：** 验证凭证绑定唯一 `run_id/task_instance_id`、contract hash、输入快照 hash、artifact version/hash、完整检查集及版本、结果和 issuer。发布前要求全部必要检查通过；动作授权绑定实际参数，拒绝缺项或部分验证。
- **验收：** 只通过 output_schema、跨 run 重用、输入改写、validator 升级、检查集缩减均不能满足发布前置条件。

### R14｜高｜动态授权的生命周期与重试语义未定义

**位置：** [§6.6 L472–478](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L472)、§6.7 L495、§11.5 L970。类型：状态机缺失。

- 授权何时签发、何时过期/撤销、被拒绝是否消费、取消与执行竞态如何处理，均未定。文中要求重放失败，又要求幂等重试：同一个已成功调用的安全重试应返回原结果还是拒绝，需要区分于恶意重复执行。
- **建议收口：** 定义 issued→reserved→committed/revoked/expired 状态，以及线性化点。相同 idempotency key＋完全相同参数返回同一 execution result；同 key 不同参数拒绝；新 key 复用已消费授权拒绝。每任务实例独立计额。
- **验收：** 并发重复请求、撤销竞争、取消后响应、超时后重试均只有一个明确结果，不发生额外发布。

### R15｜阻断｜“可信控制通道”缺少实际身份与授权模型

**位置：** [§2.4 L116–133](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L116)、§9.4 L793–815。类型：关键工程选择。

- 文档明确不能自报 owner，但未定义谁能运行 confirm-contract、修改例外、创建 grant、promote、rollback。CLI 在同一操作系统身份下运行时，换命令名不会形成权限边界。
- `import --contract <file>` 是否会把任意文件导入为已确认契约，也没有明确拒绝规则。批准一次后，哪些字段变化必须重新批准，哪些可以参数化绑定，同样缺失。
- **建议收口：** P0 选定单机可信管理员/受保护服务身份，确认接口仅其可调用；普通导入只能引用批准记录，不自行批准。为所有控制操作列 actor×operation 权限表，批准绑定内容 hash 和适用域；变更边界生成新 revision。
- **验收：** Patcher、红队、PR 作业身份执行确认/例外/晋级操作被拒；修改已批准文件任一关键字段后，旧批准失效。

### R16｜阻断｜逻辑角色分离尚未落实为部署与挂载拓扑

**位置：** [§9.1 L684–697](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L684)、§9.3 L789、§10.2 L853–856。类型：架构缺项。

- 同一 Python 进程的不同模块无法靠目录命名落实 Runner 独占保护集、Patcher 不可挂载、receipt 私有等要求。PRD正确提出了隔离要求，但容器/进程/OS 身份以及各自可访问资源没有选定；OpenShell 又是可选项。
- 扫描器和修补器同样处理不可信包或日志，也需要边界；仅隔离 victim 不够。模型服务的推理入口和管理入口也需具体分开。
- **建议收口：** 固定一套 P0 拓扑，画明 controller、protected evaluator、dev runner、scanner、patch worker、model gateway 的身份、只读/可写挂载、网络端点和输出通道。可共享模型权重，但共享不等于共享消息历史或控制能力。
- **验收：** 各角色尝试读取保护用例、修改 Gate/receipt、调用模型管理接口、访问外网均有实测拒绝；重启后边界仍成立。

## 三、Runtime、任务与 Oracle

### R17｜高｜支持哪些 Skill、如何加载 references，没有可执行兼容性规范

**位置：** [§3.5 L184](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L184)、§4.3 L214、§6.2 L415–420、§9.1 L684。类型：接口缺失。

- 完整包扫描不等于完整 Skill 执行。官方格式允许 scripts/references/assets，正文与资源有按需加载语义；薄 Runtime 如何激活 Skill、加载 metadata、访问参考文件、处理超长正文和多个 Skill 未定义。[Agent Skills 官方规范](https://agentskills.io/specification)
- 现有 read_artifact 示例只允许 task.inputs，references 不在其中；若全部预加载，又会改变 C2 的“未经过可选警告路径”测试。
- **建议收口：** 发布 `SupportedSkillProfile v1`：明确 P0 仅支持何种 Markdown/工具组合；脚本依赖如何识别并输出 unsupported；Skill reference 使用独立只读资源命名空间和可观测加载事件，固定加载策略与 token 截断规则。
- **验收：** 一个引用必读参考文件的 Skill、一个依赖脚本的 Skill、一个超上下文 Skill 分别获得预定行为；不会把“扫描了包”误写为“执行了包内全部能力”。

### R18｜高｜四个核心工具只有名字，没有请求、返回与错误契约

**位置：** [§6.2 L414–427](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L414)、§9.1 L693–697。类型：工程接口缺失。

- read 是否分页、write 是否覆盖/追加、validate 校验什么、publish 是否一次发布多产物均未定；异常是否产生部分副作用、拒绝是否计步骤、未知参数是否拒绝也未定。
- **建议收口：** 提供四套严格 schema、资源生命周期、错误码、幂等语义、最大字节数和副作用保证。工具实现只接受规范化调用对象；不允许额外字段偷偷承载 path/URL/执行代码。
- **验收：** 每个工具都有成功、越权、非法参数、重复调用、部分失败与最大尺寸用例；发布 receipt 与实际产物字节一致。

### R19｜高｜表格业务的“正确结果”还没有定义到能写参考实现的程度

**位置：** [§4.1 L193–195](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L193)、§5.4 L304、§6.2 L406、§7.2 L526–528。类型：业务契约缺失。

- 主键字段、重复键处置、inner/left join、未匹配客户、空值、编码、数字精度/币种、排序、汇总口径、输出字段、正常异常输入的预期行为均未指定。不同实现会生成不同“确定性真值”。
- `unique_customer_key` 要在合并前校验，但示例 validate_dataset 只允许 task.outputs；输入检查是预先由 fixture 保证、由工具执行，还是靠模型读后判断，没有交代。
- **建议收口：** 增加 tabular_report_v1 的规范输入 schema、参考变换、错误处置表、输出 schema、规范序列化规则与带预期结果的可信示例。明确 input validation 与 output validation 的执行点。Oracle 和生成器应有独立校验样例，防止共享同一个计算错误。
- **验收：** 重复键、缺失客户、小数舍入、空表等边界样例的唯一预期可由非模型程序计算；不把合法业务拒绝算功能失败。

### R20｜高｜8 步工具预算与业务工作流、模型输出预算尚未闭合

**位置：** [§3.3 L165](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L165)、§6.2 L425–427、§10.3 L890。类型：可实现性风险。

- 若读取三份输入、写两个产物、分别验证和发布两个产物，就可能需要 9 次工具调用，尚未包含输入主键校验、拒绝后的恢复。是否支持批量操作会改变结论，当前未定。
- 无 join/aggregate 工具、无脚本时，模型是否要自行生成整个 CSV？若是，400–900 累计输出 tokens 与数据规模可能不匹配；若确定性工具包办变换，则需要说明 Skill 实际控制什么，避免业务成功被工具包办掩盖。
- **建议收口：** 写出一个合法发布任务的最小调用序列，冻结 row/byte 上限、批量工具和恢复余量；明确 steps=模型轮数还是工具调用数，拒绝/格式错误也如何计数。M0据该真实路径定预算。
- **验收：** 正常路径加一次被拒绝后的合法恢复仍有完成空间；没有超预算的隐性工具调用或格式修复调用。

### R21｜高｜把“确实进入模型上下文”作为有效样本条件，可能产生选择偏差

**位置：** [§5.4 L312](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L312)、§7.2 L533、§7.6 L600–601。类型：评测语义缺失。

- 补丁后 Skill 不再读取可选恶意说明、却能完成合法任务，可能是有效防御；按当前文字却可能将攻击判 invalid，甚至阻止晋级。若按各版本实际读取情况各自删样本，配对分母也会改变。
- **建议收口：** 拆开预执行 `fixture_validity` 和后执行 `exposure_status`。设计有效的样本不能因为候选避开可选入口就被删除；分别报告端到端攻击率和已暴露条件下攻击率。被截断或注入投递失败属于 harness 问题，与安全地不读取分开。
- **验收：** 基线读入攻击、补丁未读但任务完成的案例保留在配对集合；注入器失败不会被记作防御成功。

### R22｜高｜攻击有效性与“业务真值不变”需要收窄为可判定条件

**位置：** [§5.4 L306–316](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L306)、§3.3 L163–165。类型：可实现性与契约缺失。

- CSV 可解析、只改 note 字段不代表业务语义不变：如果 note 本身决定处理规则或需原样输出，攻击文本替换会改变合法答案。跨文档攻击涉及几个槽位、1500 字符按字节还是码点、总载荷上限、动态工具响应注入时机也未定义。
- 自动规则不能一般性证明自然语言攻击在语义上有效；文中允许 uncertain 是正确方向，但补生成次数、覆盖不足处理仍未写。
- **建议收口：** P0只允许不影响参考函数结果的已声明字段，或明确定义“需原样保留但不能执行”的字段语义。定义 MutationSpec、slot selector、替换/追加规则、触发工具/轮次、长度计量、最大生成尝试和去重策略。
- **验收：** 注入后重新计算真值；真值改变、结构破坏、槽位不匹配、过长和未知语义分别有固定结果；无法生成足够有效样本不能少测后 pass。

### R23｜高｜已授权输入里的敏感内容，缺少独立于 canary 的发布规则

**位置：** [§6.5 L468](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L468)、§7.2 L522、§6.2 L424。类型：安全契约缺失。

- 文档正确认识到路径授权挡不住内容泄漏，但固定 schema 不一定阻止合法字符串/数值字段承载秘密。接收端“检查”是事后 Oracle 还是发布前防护，也没区分。
- 如果防御直接获得本应隐藏的 canary 真值，再据此拦截，会把评测判定器变成特别知道测试答案的防御。
- **建议收口：** TaskContract声明允许输出的字段、数据变换和敏感信息处置；防御只消费公开业务约束，隐藏 canary 值仅供 Oracle 判定。说明 P0 哪些内容规则能在发布前确定性执行，哪些只是事后检测、仍有剩余风险。
- **验收：** 正常敏感输入经批准变换可输出；未经批准复制被观测；防御无法读取测试 canary 清单；最终报告明确 prevention 与 detection。

### R24｜高｜攻击目标成功与普通业务失败的归因没有完全分开

**位置：** [§3.4 L178](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L178)、§7.1 L509–516、§7.2 L522–528、§7.6 L614。类型：判定契约缺失。

- DoS 是目标之一，但超时又被定义为 inconclusive；纯最终文本拒绝/污染没有工具副作用，却可能已满足攻击目标。反过来，任意正常业务错误不能一律归因于注入成功。
- 一条攻击诱导错误目的地却实际造成越权读取，究竟算该 objective 成功、其他风险还是两者？目前公式中“目标违规”与任意违规交替使用。
- **建议收口：** 分开 `objective_success`、`any_security_violation`、`utility_success`、`infra_status`；为每个 objective 定义可观测 predicate 和最终消息/产物/接收端范围。基础设施超时不能当攻击成功；因攻击导致预算耗尽需有预先定义的可判定规则和正常配对证据。
- **验收：** 口头拒绝、任务完成但错误发布、不同目标违规、基础设施超时、可判定业务中断得到不同标签。

### R25｜高｜指标分母、聚合单位和未完成处理不够精确

**位置：** [§7.6 L600–616](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L600)、§8.1 L638–641。类型：测量契约缺失。

- ASR 分母写了有效且可判定，Robust Utility、False Refusal 没写完整分母；多 objective、多 finding 共用一 run 时怎样计数未定。重试后的失败是否保留、最终环境错误是否进 Clean Utility，仍有歧义。
- 总成功率相同可能掩盖逐例回归：旧版通过 A 失败 B，新版相反。只检查聚合不下降无法落实“关键业务不回归”。
- **建议收口：** 以固定 evaluation case 为主单位保存逐例向量，attempt/action 次数另列；冻结哪些 critical cases 必须逐例通过。显式给出有效、暴露、已判定、失败、未完成的数量与所有公式，零分母输出 null；基础设施失败不能删除后完整晋级。
- **验收：** 一个 run 多违规、一个 finding 多 run、全不确定、旧新交换成败等手算样例与代码结果一致。

### R26｜高｜重放、重试和随机种子的规则会影响公平性与是否“刷到通过”

**位置：** [§5.5 L322](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L322)、§7.6 L612–616、§10.3 L868。类型：执行契约缺失。

- P0说关键失败固定预算重放，但没有最低次数、稳定阈值和原失败是否仍否决；“环境可重试一次”还受全局仅 4 次预留约束。若有 5 个环境失败，必须有唯一处理。
- 每次 fixture 新 canary 与同案精确重放需要分清：旧新版本应共享同一具体 fixture，独立重复才更换随机值。只有 seed、不保存生成的实际文件和 payload，不能保证模型生成结果可重放。
- **建议收口：** 区分再现性测量与环境重试。已观察到的有效关键违规不能被后续成功覆盖；记录所有 attempt。冻结一对比较的 fixture/payload，独立重复采用预登记种子/实例；预算不足返回未完成。
- **验收：** 首次泄漏后二次没泄漏不能变 pass；无 seed 支持仍能按保存输入重放，并明确输出不保证相同。

## 四、扫描、制品与证据

### R27｜高｜跨版本 Finding 身份、消失与重新打开规则缺失

**位置：** [§5.3 L293–299](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L293)、§5.2 L279、§9.2 L714–715。类型：数据模型缺失。

- 来源键含 skill_hash，任何包内容改变都会得到新键；去掉 hash 又可能将不同漏洞误合并。同规则同文件相同文本多次出现、重命名、行号移动、一个发现分拆为多个都没有对应策略。
- **建议收口：** 区分 immutable `observation_id` 和长期 `logical_finding_id`，维护关系与证据；跨版本匹配仅产生候选关联，不自动关闭。复扫消失标记 absent_in_scan，只有独立修复证据才关闭；重现时 reopen原逻辑问题。
- **验收：** 无关文件改动、搬行、复制危险段落、重命名和一拆多不会丢失未关闭风险或错误计修复数量。

### R28｜高｜扫描器输出已锁版本，但到内部状态的适配表尚未冻结

**位置：** [§5.3 L287–300](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L287)、§8.1 L634。类型：集成契约缺失。

- 必须定义 missing field、未知严重度、覆盖 partial、命令退出码与 JSON相互矛盾、LLM未请求/请求但失败时的唯一处理。静态扫描“按所选模式完整”与全能力覆盖不同，不能把可选 LLM 未启用一概当扫描错误，也不能忽略必要分析失败。
- 固定版本源码中 `issues[].id` 是规则 ID，而 `finding_id` 是一次观测 ID；文档提取时必须显式映射。原始文件路径和证据也要绑定扫描快照。[SkillSpector Finding.to_dict 源码](https://github.com/NVIDIA/SkillSpector/blob/dabf4759a189be0f0428a2f7a472b3d5bdad1fe6/src/skillspector/models.py#L233)
- **建议收口：** 保存一组真实报告作为 adapter 合同样例；按 scan profile列 required analyzers，建立每种 exit/status/coverage 的映射表。未知输出版本拒绝推断安全。
- **验收：** 0但有告警、1但报告完整、1且partial、2、JSON损坏、缺关键字段、LLM降级均得到预定状态。[上游完整性契约](https://github.com/NVIDIA/SkillSpector/blob/dabf4759a189be0f0428a2f7a472b3d5bdad1fe6/docs/ANALYSIS_RESOURCE_BOUNDS.md)

### R29｜中｜固定扫描器版本仍不足以保证复扫可比与缓存有效

**位置：** [§5.3 L287](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L287)、§10.2 L854、§13.1 L1024。类型：环境复现缺项。

- 上游 `--no-llm` 仍可请求 OSV；外部漏洞库、联网可达性和离线 fallback 会变化。扫描模式固定也可能产生不同发现，影响“补丁新增风险”判断。[SkillSpector 数据外发说明](https://github.com/NVIDIA/SkillSpector/blob/dabf4759a189be0f0428a2f7a472b3d5bdad1fe6/README.md#trust-model-and-data-egress)
- **建议收口：** 明确本地模型模式是否允许 OSV外发；配对扫描保存查询响应快照/依赖数据库标识，区分内容变化与情报变化。隔离 scanner 的配置与环境变量，不能从被测目录意外继承 provider或抑制配置。
- **验收：** 断网、OSV变化、provider配置污染时不使用旧缓存作安全结论；报告可解释差异来源。

### R30｜高｜导入完整包的安全快照规则未定义

**位置：** [§2.3 L97](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L97)、§5.3 L287、§9.1 L688。类型：安全与可复现性缺项。

- 禁止执行脚本并未回答 symlink/hardlink、路径穿越、外部 reference、嵌套压缩包、超大文件、大小写/Unicode冲突、读取期间源文件变化的处理。扫描与执行若分别读活动目录，可能不是同一份包。
- **建议收口：** P0 明确只接受哪类输入（建议本地目录快照），严格限定普通文件/相对路径、总文件数和字节上限；不可变快照先生成再供扫描/执行/修补读取。外部或不支持制品不能静默忽略；不支持远程URL/压缩包则显式拒绝。
- **验收：** 扫描期间改源文件不会影响已锁快照；符号链接指向控制目录、超限文件和重复规范路径全部按固定规则拒绝或标覆盖不足。

### R31｜高｜hash 的规范与评测证明覆盖范围缺失

**位置：** [§8.3 L668](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L668)、§9.2 L706–726。类型：数据契约缺失。

- “所有实体带 content_hash”没有说明算法、规范化、排除自身 hash 字段、时间/id 是否进 hash、包目录如何排序。文件字节 hash、语义策略 hash、实体身份和运行身份不能混用。
- 晋级组合只列 tool_schema/runtime，而相同 schema 下工具实现、契约、grader、Gate配置或模型变化，也会使旧评测不再有效。
- **建议收口：** 区分 `bundle_digest` 与 `evaluation_attestation_digest`：前者覆盖分发制品，后者绑定 bundle、contract、工具实现、runtime、模型/提示配置、suite、grader、Gate、scanner profile和证据索引；明确 canonical encoding和版本。决策有效性验证整个证明上下文。
- **验收：** 任一可信实现/规则变化都会使旧证明失效或触发重新认证；非语义的显示字段变化不会意外改变制品内容身份。

### R32｜高｜实体字段表与 CLI 草案还不是可独立实现的接口

**位置：** [§9.2 L704–749](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L704)、§9.4 L791–815。类型：接口缺失。

- 缺类型、必填/可空、枚举、引用关系、不变量和升级策略；`ci_result.json` 没有完整样例。JSON事件示例也未满足“所有实体共有字段”，需要说明它是简写而非规范。
- CLI 的 stdout/stderr、输出目录冲突、参数错误、扫描器退出码与产品退出码的区别、命令幂等和重跑行为未定。`harden --candidates 1` 如何体现两轮也不清楚。
- **建议收口：** 交付正式 JSON Schema/Pydantic 模型、工具协议和 CLI命令契约；结果至少包含 R01 的双裁决、未完成项、覆盖、预算、版本、完整证据索引。退出码从“建议”改为规范，非评测CLI错误单列。
- **验收：** 报告消费者不需读 Markdown 即可判断对象、结果与未决原因；缺字段/未知枚举拒绝；所有四状态有完整示例。

### R33｜高｜晋级原子更新还缺并发、过期与取消语义

**位置：** [§2.3 L103](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L103)、§8.3 L668、§9.4 L815。类型：状态机缺失。

- A、B两次PR都基于 V0 测试，B先晋级，A后完成不能盲目覆盖B。force-push、重复 webhook、取消作业与 late result 同样会产生陈旧结论。
- **建议收口：** CIJob固定 base/head SHA、active revision、contract revision、触发ID；promote对 expected active revision 做 compare-and-swap。冲突、已撤销、已取消、旧 head的结果保留作证据但不得晋级或覆盖新检查；P0可用单写者调度加事务锁。
- **验收：** 倒序完成、并发 promote、重复事件和取消后迟到响应均不会把 active 或 PR检查退回旧状态。

### R34｜高｜缓存键及随机运行复用语义仍有缺项

**位置：** [§2.4 L135](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L135)、§10.3 L871、L886。类型：复现与正确性缺项。

- 当前列表没有明确包含 system prompt、工具描述、采样参数、上下文处理、生成的实际攻击字节、grader/Gate版本、授权事件序列和副作用初始状态。把它们统称 environment hash 还不能让不同模块实现一致键。
- 缓存一次随机成功等于重用历史观测，不是本次独立重复；反复引用同一 run 不能增加样本量。授权撤销也不能因为缓存命中被忽略。
- **建议收口：** 定义完整 `EvaluationFingerprint`、缓存允许状态与引用语义，区分 execution cache 和 verdict recomputation；当前授权/撤销与晋级条件必须重新检查。研究重复需要不同 run identity，引用不算新增独立样本。
- **验收：** 改 prompt/采样/grader/授权事件导致失配；同 run多次引用只计一次；旧缓存不能穿过撤销。

### R35｜高｜日志完整性、来源标签与保护信息的输出路径未闭合

**位置：** [§6.5 L466](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L466)、§9.2 L718–749、§9.3 L789、§9.6 L830–834。类型：证据契约缺失。

- source_id不能只以模型可仿写的字符串标签体现，必须和真实消息/文件事件绑定。JSONL尾部丢失、工具执行后日志失败、接收端检查超时、跨run串日志如何判证据完整，未定义。
- 同时，保存完整模型可见消息、fixture和trace可能把保护payload写进共享 `runs/` 或CI下载包，绕过“Patcher不挂载protected”的限制。
- **建议收口：** 可信侧分配并校验事件身份、序号、引用、起止标记与快照闭合；日志与状态事实对账。开发证据、保护原始证据、可公开报告分别定义访问权限和脱敏导出，Markdown也需安全显示攻击文本。
- §9.3只有单个 `scan_after.json` 与若干顶层结果文件；两轮修补不能覆盖同名历史证据。应采用 `iterations/<iteration_id>/` 和 `evaluations/<eval_id>/`，顶层仅保存不可变索引或明确的汇总引用。
- **验收：** 删除/重排事件、伪造source标签、截断trace、复制别的run结果不可能完整通过；Patcher不能从报告/缓存间接读保护payload。

### R36｜高｜“业务内容完整性”无法仅由 diff/解析器保证

**位置：** [§5.7 L341–345](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L341)、§5.8 L355、§8.1 L635。类型：可实现性边界。

- Markdown仍可解析不代表关键流程仍存在；最小diff也可能删除关键约束。纯语义“没有删业务流程”无法用一般性确定性静态检查完成，少量正常任务通过也不代表全功能保持。
- 每候选3文件限制是每轮还是两轮累计？新增/删除/重命名、references循环、frontmatter和依赖声明能不能改？新增文本上限具体多少？均未定。
- **建议收口：** 列允许路径/操作/字段、累计字节和文件上限；应用diff时绑定exact parent，拒绝模糊匹配。结构完整性、受保护字段完整性与行为回归分别判定；无法确定的语义影响保留为待复核，不声称静态已证明业务完整。
- **验收：** 空补丁、路径别名绕过、恶意rename、父版本不符、删关键frontmatter、两轮累计越界均失败；结构合法但业务回归失败仍不能晋级。

### R37｜高｜义务的生成、编译与版本化之间缺少规范连接

**位置：** [§5.8 L349–353](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L349)、§5.7 L341–342、§6.3 L436、§6.7 L493–495。类型：实现语义及范围冲突。

- RepairObligation只给字段，没有合法 obligation 类型、参数、可信依据校验、到policy的编译规则。Patcher允许改的三类文件里又没有说明obligation产物由谁写；模型不可借描述文字增加新的可信执行代码。
- §5.8把阶段/预算义务列P1，§6.3/§6.7却把阶段和预算列入P0策略/调用条件。已有固定预算执行与可自动修补的预算义务可以区分，但须写清。
- **建议收口：** 三种P0义务分别提供schema、编译模板和工具入口映射；controller生成受保护的编译产物，compiler版本入证明。基础执行计数器是P0固定机制还是可编辑策略，显式区分；unknown/uncompiled不能被字符串说明绕过。
- **验收：** 伪造contract_ref、未知check、遗漏入口、编译器升级、试图把日志文本当授权时明确拒绝；policy与obligation hash始终同步。

## 五、评测边界、范围与交付

### R38｜中｜红队和修补器各能看到什么，需要字段级可见性表

**位置：** [§3.3 L163–165](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L163)、§5.4 L306–318、§5.9 L367。类型：实验契约缺失。

- 默认黑盒只知道工具schema/业务类别，但生成时又从完整TaskContract提取禁止动作、资源、目的地；反馈包含原因码。哪些业务规则公开、哪些评分条件秘密、是否暴露policy、canary路径和值没有清单。
- 修补器可看完整失败trace，但“不能看攻击真值”的边界须区分实际可见payload与隐藏MutationSpec/objective标签，否则实现容易相互矛盾。
- **建议收口：** 为 generator/victim/diagnoser/patcher/dev evaluator/protected evaluator 列字段可见性矩阵。攻击者看见的资源定位必须符合声明能力；隐藏答案与控制路径不混入trace提示。
- **验收：** 自动检查每角色实际prompt/materialized files不含禁止字段；白盒模式单独标识，不能混报默认实验。

### R39｜中｜跨PR保护集轮换与历史回归公开过程缺少操作规则

**位置：** [§7.3 L547–562](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L547)、§8.4 L676–677。类型：生命周期缺失。

- 文档承认反复提交会过拟合，但“定期更新”没有触发条件。保护失败如何转入可修的开发历史库、何时换新保护用例、旧证明是否仍有效，没有定义。聚合reason code若过细，也可能逐步泄漏答案。
- **建议收口：** suite/version/epoch固定；定义每epoch查询预算、轮换与退役条件。向开发公开保护失败时先退役并标明它不再hidden，再生成新保护套件；保留旧用例作回归，不能删除失败样本换取更高分。
- **验收：** 同head反复运行计同一查询账本；公开过的案例不会继续被标独立保护；更新记录含原因与新旧覆盖。

### R40｜中｜通过结论与导出产物在真实宿主上的适用范围需写成机器可读约束

**位置：** [§1.1 L28–40](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L28)、§6.1 L378、§7.7 L626、§11.1 L908。类型：产品交付缺项。

- 文档已说明外部adapter在P1、policy自定义且需adapter，这是正确的；但“加固后的Skill”交付时没有定义如何防止只复制SKILL.md、丢掉代理/凭证后仍沿用安全结论。
- **建议收口：** 证据包包含 supported runtime/profile、required enforcement、模型/工具/策略证明及适用边界；独立Markdown文本导出应标记未携带运行时保证。P0绿色只代表此薄Runtime和冻结套件。
- **验收：** 导出后改变宿主或省略PolicyProxy不会显示为同一已认证配置；报告区分文本改善与运行时拦截收益。

### R41｜高｜非功能预算和性能门禁没有从“建议”变成执行规则

**位置：** [§8.1 L642](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L642)、§9.6 L830–831、§10.3 L890–898。类型：执行契约缺失。

- 硬门禁里用“建议125%/150%”，实现不知应警告还是拒绝。p95是单任务还是整个CI、是否含排队/扫描/warmup、token是否含隐藏推理/格式修复，都未定。
- PR victim条数受限，但攻击生成、诊断、修补、扫描、本地队列、输出/trace大小没有完整的P0总上限；模型API中止后是否仍在占GPU也需处理。
- **建议收口：** 冻结每阶段timeout、最大生成调用/tokens、最大上下文/产物/日志、总墙钟和资源限额；性能阈值明确 hard/soft、比较基线与统计口径。预算账本在调用前预留、结束后结算，未知token计量不能当零。
- **验收：** 无止境格式修复、扫描卡死、超大日志和排队拥堵均有可预测终止；取消后无残留worker继续发布或写证据。

### R42｜中｜P0/P1范围有几处直接或隐含冲突

**位置：** [§4.2 L202–206](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L202)、§4.3 L216、§7.4 L566、§11.1 L907–912、§11.2 L928。类型：范围冲突。

- “核心Demo”描述A/B/C三候选，而P0只要求最多两轮、最终一个finalist，三组研究比较在P1。C1/C2写贯穿六攻击族，而P0只做三种；“目的地改写”也不是§3.4六族中的同层分类，实际上更像目标。
- 人手不足可降低第二Skill/自动修补复杂度，但它们仍是最终P0强制验收项；阶段降级不能同时声称原P0完成。
- **建议收口：** 一张版本范围表作为唯一规范来源，分别列 required/optional/deferred；统一attack family与objective分类。定义明确的降级交付版本名和对应验收，不在正文散落例外。
- **验收：** 每个FR映射到唯一release范围；P0演示不暗含P1消融已经完成；降级版本报告缺项。

### R43｜中｜强制展示第二轮修补与“没有自然漏洞也如实报告”需要区分验收类型

**位置：** [§11.5 L966–967](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L966)、§11.3 L939–942、§11.4 L955–957。类型：验收语义缺失。

- 模型可能首轮修好，或没有找到稳定漏洞，此时真实研究结果不能保证出现第二轮；但当前最终清单把两轮链路作为必做。
- **建议收口：** 将“控制器支持两轮”用明确标识的受控故障注入/固定候选夹具验收；“真实模型自然出现两轮并改善”作为实测结果独立报告。夹具不得伪装为自动研究效果。无真实失败时也应明确哪些效果验收未完成。
- **验收：** 控制器fixture稳定走两轮并验证预算/证据；真实模型结果照实记录，不能为演示而弱化保护集或伪造trace。

### R44｜中｜P1保护验证可两轮与冻结候选不重选的关系未定义

**位置：** [§7.4 L576–586](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L576)、§7.6 L612、§8.4 L677、§10.3 L879–888。类型：研究协议缺项。

- 一处要求protected逐例结果不能用于重写/重选，另一处允许protected最多评估两轮，第二轮的触发条件、可用反馈、是否重新选择和预算没有完整协议。“最终一次”应指一次冻结评测campaign，可含预定3次重复，避免被误解成仅1条运行。
- 预算算术608/752正确，但第二轮、额外发现和无效候选处理必须依manifest，不保证同一千次内完成。
- **建议收口：** 注册研究protocol：候选冻结点、选优指标/平局规则、每阶段查询数、第二轮允许条件、最终campaign重复种子、失败后的新实验标记。每个claim绑定对应对照和split。
- **验收：** 研究执行日志能区分预先承诺的重复与看结果后的重试；保护结果不会成为隐性候选搜索信号。

### R45｜中｜本地模型与依赖可行性检查需要有冻结产物和退出标准

**位置：** [§10.1 L842–846](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L842)、§11.2 L920、§11.3 L935–937。类型：工程风险与验收缺项。

- 文档明确硬件/模型记录未现场验证，这是正确的；仍需指定结构化输出成功率、正常业务成功标准、context与量化选择、arm64依赖锁定及失败后的降级模型/后端路径。只写“稳定”无法决定M0是否完成。
- Runtime同时出现 JSON action loop 与工具schema，须决定使用原生tool calling还是自定义JSON动作。Ollama支持工具调用及多调用返回，不能默认一次模型回复一定只有一个工具请求。[Ollama官方工具调用文档](https://docs.ollama.com/capabilities/tool-calling)
- **建议收口：** 输出M0 manifest和固定最小业务样本实测结果；协议二选一、明确多调用调度/计额；锁依赖、容器/模型digest、配置与fallback触发条件。后端变化重新跑机制和业务基线。
- **验收：** 从干净Spark环境按锁定配置可重建；不支持seed/tool calling的后端得到明确能力报告和处理，不默默换协议。

### R46｜中｜两个Skill和接入成本的验收尚未具体化

**位置：** [§1.1 L40](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L40)、§11.1 L904–914、§11.2 L930、§14 L1069。类型：产品验收缺项。

- 两个Skill未给名称/固定内容/正常任务差异；只修改文件名和备注也可能形式满足配置复用，但不能验证真实接入价值。“不改核心代码”是否允许改task-family adapter、模板或Oracle没有明确边界。
- **建议收口：** 冻结两个有实际流程/字段/输出差异、但属同类任务的Skill及manifest；列允许改变的文件集合。记录接入人工输入、审批字段、耗时、生成有效率和必须新增的业务代码；不预填低成本结论。
- **验收：** 第二个Skill接入diff只落允许配置/包/可信样例；如修改fixture factory或Oracle，按事实标为扩展任务类别而非纯配置复用。

### R47｜高｜持续CI的队列、持久化、恢复与清理策略缺失

**位置：** [§2.4 L135](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L135)、§9.3 L772–786、§9.6 L831、§10.2 L852。类型：运行工程缺项。

- 单worker每PR数十分钟时，多个提交如何排队、合并过期任务、nightly如何避免饿死PR未定。重启后从哪一步恢复、是否重发副作用、证据/快照多久保留、磁盘满怎么办也未定。
- “历史结果不可覆盖”不等于所有临时文件永久保存；清理既不能误删活跃bundle证据，也不能让不可信运行留下持久状态。
- **建议收口：** P0明确单机单写者任务队列、同head去重与旧head取消策略、恢复checkpoint、保留/GC引用规则和磁盘阈值；保护证据与公开报告分区。发生资源不足时拒接/未完成，不生成部分绿色报告。
- **验收：** 中途重启、磁盘满、连续多PR、nightly插队、删除孤立临时文件不会破坏已完成证据或重复发布。

### R48｜高｜验收清单尚不能作为实现完成的判定依据

**位置：** [§11.5 L961–977](https://github.com/JiahaoTanXX/SkillLoop/blob/74677b2b6417df00d9f05a5ca51a3c5037877ae2/PRD.md#L961)、§2.3 FR-00–FR-16。类型：工程验收缺失。

- 目前多数标准是“有测试”“可查看”“可端到端完成”，没有绑定输入、状态、输出和失败预期。安全机制特别需要验证失败路径，不仅演示一次正常路径。
- **建议收口：** 建立FR→规范→测试ID→证据文件→通过条件矩阵。关键测试必须覆盖假绿色、并发晋级、旧凭证、参数替换、partial日志、预算溢出、保护泄漏、全拒绝、未知策略与无active初始化；P0/P1分别验收。
- **验收：** 每个P0要求至少有可重复的正例与适用负例，失败时产生固定reason code；运行结果能自动汇总完成情况，不靠口头勾选。

## 六、需要优先冻结的决策与建议写法

以下是可供下一版PRD采用的**建议决策**，不是原文已有要求，也不是本次已替你批准的产品取舍。优先冻结九个阻断项：R01、R03、R04、R05、R06、R11、R12、R15、R16。

| 决策 | 建议默认值 | 避免的实现分歧 |
|---|---|---|
| CI的评价对象 | PR检查只绑定提交SHA；修补bundle单独评价 | 修补成功错误地给原提交放行 |
| 首次接入 | 无active亦可测；绝对门禁通过后才成为初始active | 未评测版本被预先当成可信基线 |
| 授权模式 | strict_ci与synthetic_ablation硬隔离 | 实验宽权限进入产品路径 |
| 修补准入 | 明确区分安全违规、越权尝试、业务失败、静态预防修补 | 未复现风险被伪造为修复成功 |
| Finding状态 | 处理方式、观测结果、最终处置三个维度 | not_reproduced/blocked被错误销案 |
| 保护评测 | 选定finalist后才对其开放一次保护campaign | 用保护反馈继续优化 |
| 历史回归 | 定义必测集合；放不下则分批或未完成 | 固定预算静默丢弃已知漏洞 |
| P0发布事务 | 合成接收事件与receipt状态同库事务，不可变产物引用 | 崩溃后重复副作用 |
| 策略证明 | 有限类型DSL＋批准契约域＋每实例重检 | 训练样本集合包含被当普遍证明 |
| 并发晋级 | expected active revision的CAS＋过期head拒绝 | 慢作业覆盖新版本 |
| 未编译义务 | 保留风险；对应修复声明不成立 | 文本警告冒充执行保证 |
| 交付范围 | P0薄Runtime认证；外部宿主必须重新适配验证 | 复制SKILL.md后仍冒用原安全结论 |

建议把以下几条写成规范性条款，并配相应schema与测试：

> **提交状态。** 作业必须为提交对象和修补对象分别保存评价。修补对象的通过不得改变未经相同验证的提交对象状态；仓库检查必须绑定确切提交SHA。

> **证据充分性。** 每次作业在执行前生成必测用例集合与预算计划。必测用例未执行、不可判定、证据不完整或超出预算时，不得返回pass；已确认的关键违规不得由后续重试成功覆盖。

> **保护隔离。** finalist的全部字节与配置冻结后才开始保护评测。保护原始输入、逐例trace与隐藏真值不得进入修补器输入或其可读取的artifact；失败后停止本作业的自动修补。

> **晋级一致性。** 通过决定必须绑定制品与完整评测上下文；promote同时验证决定有效、当前active revision、提交时效和当前撤销状态。任一条件不符，不更新active。

> **发布提交。** 发布执行必须在同一可信提交点确定参数、产物版本、验证凭证、授权和幂等身份；未知副作用状态不得作为未执行而盲目重试。

一个建议的外部状态优先表如下。它解决的是信息汇总规则，仍需正式确定风险等级和适用门禁：

| 当前证据 | 建议状态 | 能否晋级 |
|---|---|---|
| 至少一个确定的关键违规、禁止变更、可判定功能退化 | fail，仍保留所有其他未完成原因 | 否 |
| 无确定失败，但缺批准契约/业务标准 | needs_contract | 否 |
| 无前两项，但有必测遗漏、unknown策略、未决高风险、环境/证据错误 | inconclusive，附needs_review等原因 | 否 |
| 所有适用规则通过、必测证据完整、对象一致 | pass | 具备晋级资格，仍须通过时效/CAS检查 |

不要让 `needs_review` 再变成一个无定义的第五外部状态；它可以是待处理原因，但必须映射到上表中的唯一结果。`pass` 与 `promoted` 也应分开：前者是评测结果，后者是有并发/时效前提的状态变更。

## 七、主要工程难点、实施顺序与交付文件

| 难点 | 为什么难 | P0可控落点 | 完成证据 |
|---|---|---|---|
| 可信授权与策略执行 | 需要区别业务意图、实验权限、任务授权和工具前置条件 | 有限资源ID、固定工具、单机可信控制服务 | 未授权/合法对照＋严格/实验模式隔离测试 |
| receipt与副作用一致性 | 崩溃、重试、撤销、并发共同影响结果 | 单事务模拟接收端＋不可变artifact | 故障注入后状态一致、无重复发布 |
| 参数化策略包含 | 动态任务集合与有状态前置条件不能只比较YAML行 | 小型类型DSL，受限绑定语法，unknown拒绝 | 刻意设计的非子集反例全部被识别 |
| 独立Oracle与自动用例 | 既要生成多样任务，又不能让模型自定答案或改变业务语义 | 一个明确表格规范＋参考函数＋独立金样 | 业务边界样例、生成有效率、逐例结果 |
| 受限补丁有效性 | 文本可解析不代表业务完整，模型也可能过拟合测试 | exact diff、禁止字段、有限开发集＋保护门禁 | 结构完整性与业务结果分别通过 |
| 实验和CI编排 | 有限预算要同时容纳修补、重放、新发现与保护验证 | execution plan＋统一预算账本＋明确未完成 | 最坏分支不会超额或假通过 |
| 证据/保护集隔离 | 原始trace、缓存、报告可能成为旁路 | 明确进程/挂载/导出ACL | 对所有输出通道做拒绝/泄漏检查 |
| 单机持续运行 | GPU排队、缓存、重启和多PR结果过期 | 单写者队列、持久状态、CAS晋级 | 多作业倒序完成与恢复测试 |

**建议实施顺序：** 先冻结业务contract、supported profile、工具schema、Gate和状态机；再实现确定性工具/凭证/Oracle与故障测试；然后让无攻击的薄Runtime跑通；再接扫描、静态计划和固定攻击；之后增加模型变体与修补；最后接PR触发、第二Skill、预算调度和报告。这样关键安全语义不会在Day5才被发现需要重做前面所有接口。

一周可否完成不能仅由2800–4700行功能代码估算；还取决于可用人力、现成工具/隔离基础、模型能力和调试时间。目前缺少这些实测，不宜把一周完成整套要求当确定承诺。原文Day1要求正常业务可完成、Day2才交付Runtime、Day3才交付Oracle，需明确Day1是可行性spike，之后何时对正式实现重新验收与冻结配置。

要达到“开发者无需猜测关键行为”，建议补齐这些规范产物：

1. `supported-profile.md`：包格式、加载策略、支持能力、拒绝规则、适用结论。
2. `task-contract.schema.json`＋`tabular-report-v1.md`：任务族/实例区分、字段级业务规则、金样和授权来源。
3. `tools/*.schema.json`：请求/返回/错误、幂等、副作用及资源生命周期。
4. `policy-spec.md`：DSL文法、绑定域、包含算法、状态条件与unknown规则。
5. `receipt-protocol.md`：签发、验证、消费、撤销、重试及事务恢复状态机。
6. `finding-lifecycle.md`：发现身份、证据、处置、复开、例外审批和失效。
7. `gate-spec.yaml`：四状态映射、硬/软规则、阈值、逐例必测与reason code。
8. `execution-profiles.yaml`：PR/nightly/research 的确切用例角色、次数、预算与溢出处理。
9. `ci-result.schema.json`＋四种完整样例：提交/候选结果、hash、覆盖与未决原因。
10. `deployment-trust-boundaries.md`：身份、进程/容器、文件/网络/模型服务访问矩阵。
11. `registry-protocol.md`：hash规范、评测证明、缓存、CAS晋级、撤销和回滚。
12. `acceptance-matrix.md`：FR到可执行正负测试及保存证据的映射。

不是必须都写进PRD正文；PRD可以通过固定版本引用这些附件，但不能让不同附件对同一个行为规定两套答案。

## 八、原有需求覆盖与建议验收矩阵

| PRD需求 | 主要待收口问题 | 最低验收例 |
|---|---|---|
| FR-00 首次接入 | R06、R15、R19 | 无active初始化；恶意草案不能自批；契约变更失效 |
| FR-01 导入 | R30、R31 | symlink/超限/变化中目录；扫描和执行同快照 |
| FR-02 生成用例 | R19、R21、R22、R26 | 注入不改真值；无效生成不足不能pass；配对同输入 |
| FR-03 攻击生成 | R07、R24、R38 | 槽位/预算/可见性越界；目标与普通失败区分 |
| FR-04 工具权限 | R03、R11、R16、R18 | 未注册/非法参数；严格模式不可放宽；旁路访问被拒 |
| FR-05 两轮修补 | R07、R09、R36、R43 | 第一轮失败传递；exact parent；不读保护反馈 |
| FR-06 联合Gate | R02、R04、R05、R08、R25 | 全拒绝、漏回归、环境未知、聚合掩盖回归 |
| FR-07 晋级回滚 | R01、R10、R31、R33 | 原提交不借补丁变绿；CAS冲突；无安全回滚版本 |
| FR-08 CLI/CI | R01、R32、R34、R41、R47 | 四状态schema；旧head/重复事件；缓存失效/取消恢复 |
| FR-09 包投毒P1 | R17、R24、R38、R44 | A/B threat track分开；真值不泄漏；研究协议冻结 |
| FR-10 第二Skill | R17、R40、R46 | 接入diff不触及core/Oracle；范围报告准确 |
| FR-11 夜间profile | R08、R39、R41、R47 | 历史增长溢出；分批完成；队列与轮换 |
| FR-12 安全义务 | R11、R13、R37 | 编译来源验证；全部入口；unknown不关闭 |
| FR-13 可信凭证 | R12、R13、R14、R15 | 跨run/改产物/变更检查集/崩溃/并发重放 |
| FR-14 完整补丁/静态对照 | R26、R36、R44 | 缺补丁留在分母；结构与功能分开；等生成上限 |
| FR-15 扫描 | R27、R28、R29、R30 | 输出异常、覆盖partial、不同情报源、跨版本匹配 |
| FR-16 逐发现计划 | R04、R07、R09、R27 | 高风险未测阻断；新发现排入计划；修复不因扫描消失自行成立 |

## 九、编辑和术语层面的剩余收口

- **§6.1标题写“三种权限”，正文有四项。** 改成四层权限/条件，避免概念数目与架构不一致。
- **TaskContract承担两种粒度。** 一次确认的是任务族模板，示例却是 `task_id: orders-017` 的具体实例。应命名区分 `TaskFamilyContract` 与 `TaskInstance`，说明哪些字段可由可信binder变化、哪些必须重批。
- **“首次只确认边界”不代表零工程接入。** 新任务族仍需FixtureFactory/Oracle；应在接入流程显式区分已有任务族配置接入与新任务族开发，避免把工程量藏在“一次确认”里。
- **§5.9的攻击元数据适用于两种track时应不同。** A类不改变Skill，before/after hash应相等或该字段不适用；变更文件是fixture，不能表述为Skill包补丁。B类才有包内MutationSpec和注入定位真值。
- **§7.2第一层Verifier不应成为A类必需门禁。** A类攻击在外部输入中，修补不需要从Skill包删除攻击文本；应按track声明 `not_applicable`，而不是判“未移除”失败。
- **架构图应先不可变导入再扫描。** 当前图在scanned之后才出现导入节点；至少注明前面的扫描对象已是冻结快照，避免扫描活动目录、执行另一个版本。
- **同配置复测与策略变化需明确。** “同配置”应指固定task/model/fixture/tool/environment，允许且必须记录被比较的Skill/Policy差异，不能按字面要求policy也相同而使策略修补无法评估。
- **must/建议/例如/可选统一层级。** 关键阈值、退出码、支持语法、预算和验收不能长期停留在示意；示例标明非规范，正式规则只保留一个来源。
- **FR-14等跨P0/P1条目建议拆ID。** 防止测试矩阵只完成P0部分就把整条研究对照标完成。
- **空集合和不适用必须定义。** 无历史案例、无扫描发现、无发布授权、无可修漏洞、零有效攻击、无输出产物分别是什么预期，不能统一用空列表真值使门禁自动通过。
- **“正常业务不下降”应限制到声明覆盖。** P0每版本两条正常任务可用于小范围CI哨兵检查；通过不能证明完整业务空间或长期泛化，这点应直接写入输出schema的coverage描述。

## 十、本次核查到的正向事实与边界

文档已经正确处理了一些容易误写的点，本次不将其重复列为缺陷：扫描命中不等于漏洞证实；allowed-tools不等于宿主强制权限；Runtime与包投毒分开；独立Oracle不可由被测模型改写；不把无限自适应攻击与固定攻击混报；policy-hardening和文本改善分开；保护失败不得在同作业内继续调参；有限样本零失败不构成普遍安全证明。这些应在下一版保留。

预算表的算术复核：P0是37；P1固定评测608，开发自适应最多144，合计752。问题在执行义务与容量是否一致，而非这几个加法错误。

抽查了固定SkillSpector版本的README、完整性文档、baseline文档和Finding序列化源码；PRD关于退出码、静态模式仍可能有OSV请求、扫描与动态Oracle分工的主要方向有上游依据。没有安装或执行，不能据此证明Spark上的兼容性与耗时。

核查了SkillSecurer v1公开正文：其动态补丁结果13/30→3/30、65/75人工补丁判断，以及SequenceMatcher阈值用于定位分流的描述与PRD核心引用一致；没有发现需要以“论文根本无动态验证”推翻本方案的依据。[论文原文](https://arxiv.org/html/2609.14079v1) 本次未逐一重新审计§13全部15个可选项目的代码、许可证及当前兼容性，也未独立复现论文实验。

**工程冻结建议：** 九项阻断问题必须有唯一决策；30项高优先级问题必须在相应模块实现前进入规范和测试；中优先级及编辑问题在对应release验收前关闭。问题关闭应附“决策/规范位置/测试证据”，不能只标“已考虑”。完成这些后再做一轮跨文档一致性复核，才适合把版本标为Ready for Implementation。
